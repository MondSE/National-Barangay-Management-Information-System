import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "NBMIS",
  description:
    "The National Barangay Management Information System (BMIS) is designed to assist barangay officials in maintaining accurate records of residents, tracking violations, and managing tax-related data efficiently. This platform aims to digitize barangay operations for a smoother, faster, and more transparent governance.",
  authors: [{ name: "Almond Tuazon" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
